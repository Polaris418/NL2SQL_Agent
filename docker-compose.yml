services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./metadata.sqlite3:/app/metadata.sqlite3
      - ./backend/tests/fixtures:/app/backend/tests/fixtures

